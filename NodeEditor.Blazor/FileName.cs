using NodeEditor.Blazor.Services.Editors;
using NodeEditor.Net.Records;
using System;
using System.Collections.Generic;
using System.Text;

namespace NodeEditor.Blazor
{
    public static class CustomEditorHints
    {
        //extension(CustomEditorHint hint)
        //{
        //    public static CustomEditorHint<BoolOptions> Bool(BoolOptions? defaultValue = null) => new CustomEditorHint<BoolOptions>("Bool", defaultValue ?? new BoolOptions());
        //}
    }
}
